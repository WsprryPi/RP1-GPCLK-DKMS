{
  "SPDX-License-Identifier": "MIT",
  "schemaVersion": 1,
  "release": "0.0.0-phase5.26",
  "outputEnabled": false,
  "rowOrder": [
    "current-supported-kernel", "prior-supported-kernel-downgrade", "newer-unknown-kernel",
    "signing-not-enforced", "signing-enforced-enrolled-key", "deliberate-build-failure",
    "deliberate-signature-rejection", "missing-headers", "overlay-or-resource-conflict",
    "interrupted-upgrade", "stale-manifest", "corrupted-archive-or-dtbo",
    "removal-inactive", "removal-open-or-active", "reinstall-after-removal"
  ],
  "rows": [
    {
      "id": "current-supported-kernel", "representativeClass": "current supported Raspberry Pi OS kernel",
      "selection": "Newest kernel explicitly supported by the release manifest on a Pi 5 identity covered by that manifest.",
      "preconditions": "Clean inactive baseline, exact headers present, sealed candidate verified, no conflicting route, output disabled.",
      "failureInjection": "none",
      "expected": {"compatibilityState":"Compatible-unqualified","reason":"exact-supported-identity-lifecycle-pass","liveEligible":false,"transactionState":"complete","retainedPriorVersion":"not-applicable"},
      "cleanupResult": "Candidate unload/unbind and test-owned cleanup complete; baseline restored.",
      "diagnostics": ["exact kernel/header/module/UAPI/DTBO/manifest identities", "output-disabled query and terminal reason", "cleanup latch false", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Only attempt-owned staged, DKMS, module, overlay, journal, and evidence state during the attempt.",
      "finalState": "Inactive, output disabled, no test-owned installation or runtime binding remains.",
      "residueAudit": "No package-owned residue and no unexplained delta; unrelated baseline hashes unchanged."
    },
    {
      "id": "prior-supported-kernel-downgrade", "representativeClass": "prior supported kernel for downgrade",
      "selection": "A manifest-listed earlier supported kernel with an exact complete prior release available.",
      "preconditions": "Current complete version inactive; predecessor sealed and verified; both kernel/header identities recorded.",
      "failureInjection": "Explicit downgrade selection; no synthetic failure.",
      "expected": {"compatibilityState":"Compatible-unqualified","reason":"exact-prior-identity-downgrade-pass","liveEligible":false,"transactionState":"complete","retainedPriorVersion":"Retain the superseded complete version until downgraded successor verification and cleanup pass; then remove only as authorized."},
      "cleanupResult": "Downgrade completes output-disabled; transaction and predecessor ownership audit complete.",
      "diagnostics": ["both release and kernel identities", "downgrade checkpoint history", "cleanup latch false", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Only versioned package-owned artifacts and exact transaction state.",
      "finalState": "Declared downgraded version inactive and complete; no mixed-version bytes.",
      "residueAudit": "No superseded test-owned residue or unexplained delta after authorized cleanup; administrator and unrelated bytes unchanged."
    },
    {
      "id": "newer-unknown-kernel", "representativeClass": "newer unknown kernel for demotion behavior",
      "selection": "A newer kernel identity absent from all allow rules in the sealed manifest.",
      "preconditions": "Unknown identity proven by exact manifest evaluation; output disabled; prior complete version recorded if present.",
      "failureInjection": "Present the unknown kernel identity to preflight and compatibility evaluation.",
      "expected": {"compatibilityState":"Unavailable","reason":"unknown-kernel-identity","liveEligible":false,"transactionState":"rejected-before-install","retainedPriorVersion":"Retained unchanged when present; otherwise not-applicable."},
      "cleanupResult": "No load, bind, route activation, or candidate installation; staged bytes removed.",
      "diagnostics": ["unknown kernel and manifest identities", "explicit demotion reason", "cleanup latch false", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Evidence and removable staging only.",
      "finalState": "Prior complete inactive state retained; unknown combination non-live.",
      "residueAudit": "No candidate residue, fallback, or unexplained delta."
    },
    {
      "id": "signing-not-enforced", "representativeClass": "signing not enforced",
      "selection": "Representative supported kernel whose effective module-signing policy does not enforce trusted signatures.",
      "preconditions": "Policy evidence captured; exact supported identity; output disabled.",
      "failureInjection": "none",
      "expected": {"compatibilityState":"Compatible-unqualified","reason":"supported-identity-signing-not-enforced","liveEligible":false,"transactionState":"complete","retainedPriorVersion":"not-applicable"},
      "cleanupResult": "Output-disabled lifecycle and complete attempt cleanup pass.",
      "diagnostics": ["effective signing enforcement", "module signer and taint state", "cleanup latch false", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Attempt-owned versioned package state only; no trust-store change.",
      "finalState": "Inactive with baseline trust configuration unchanged.",
      "residueAudit": "No signing keys, trust entries, package residue, or unexplained delta."
    },
    {
      "id": "signing-enforced-enrolled-key", "representativeClass": "signing enforced with enrolled key",
      "selection": "Representative supported kernel enforcing signatures with an administrator-owned enrolled local key.",
      "preconditions": "Enrollment and signer fingerprints recorded; private key excluded from evidence; output disabled.",
      "failureInjection": "none",
      "expected": {"compatibilityState":"Compatible-unqualified","reason":"trusted-local-signature-lifecycle-pass","liveEligible":false,"transactionState":"complete","retainedPriorVersion":"not-applicable"},
      "cleanupResult": "Signed candidate removed after output-disabled verification; administrator key and enrollment retained.",
      "diagnostics": ["enforcement and enrolled certificate fingerprints", "module signer and signature status", "cleanup latch false", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Attempt-owned signed module and package state; no enrollment mutation.",
      "finalState": "Inactive; administrator-owned key and trust state byte-for-byte preserved.",
      "residueAudit": "No private key in evidence, no package residue, and no unexplained delta."
    },
    {
      "id": "deliberate-build-failure", "representativeClass": "deliberately failed build",
      "selection": "Supported representative headers with a bounded disposable build-failure injection.",
      "preconditions": "Prior complete version inactive and recorded; injection cannot alter source or system headers.",
      "failureInjection": "Deterministic compiler wrapper exits nonzero at DKMS build.",
      "expected": {"compatibilityState":"Unavailable","reason":"dkms-build-failed","liveEligible":false,"transactionState":"failed-recovery-required","retainedPriorVersion":"Retained complete and unchanged."},
      "cleanupResult": "Failed successor build tree and registration removed; prior version remains inactive and usable under its own identity.",
      "diagnostics": ["exact compiler/header identity and bounded build log", "failure checkpoint and exit status", "cleanup latch false after recovery", "prior-version identity"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Disposable build, DKMS registration, transaction journal, and evidence only.",
      "finalState": "Prior complete version retained; failed successor absent and non-live.",
      "residueAudit": "No failed build/install residue and no unexplained delta."
    },
    {
      "id": "deliberate-signature-rejection", "representativeClass": "deliberately rejected signature",
      "selection": "Signature-enforcing representative kernel with an enrolled trusted key and a candidate signed by a different or corrupted signer.",
      "preconditions": "Trusted and rejected signer fingerprints recorded; prior complete version inactive if present.",
      "failureInjection": "Attempt verification/load with wrong-signer or byte-corrupted signature in separate attempts.",
      "expected": {"compatibilityState":"Rejected","reason":"signature-rejected","liveEligible":false,"transactionState":"failed-recovery-required","retainedPriorVersion":"Retained complete and unchanged when present; otherwise not-applicable."},
      "cleanupResult": "Rejected module never binds; failed successor removed without trust-store mutation.",
      "diagnostics": ["enforcement, trust, signer, and module hashes", "verification/load rejection and bounded kernel log", "cleanup latch false after recovery", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Attempt-owned signed bytes, journal, and evidence only.",
      "finalState": "Rejected candidate absent; prior inactive state retained.",
      "residueAudit": "No rejected module, key, trust, package residue, or unexplained delta."
    },
    {
      "id": "missing-headers", "representativeClass": "missing headers",
      "selection": "Representative installed kernel with its exact build headers deliberately absent.",
      "preconditions": "Absence proven read-only; no package-manager mutation is part of this row.",
      "failureInjection": "Run preflight with exact kernel headers unavailable.",
      "expected": {"compatibilityState":"Unavailable","reason":"headers-missing","liveEligible":false,"transactionState":"rejected-before-build","retainedPriorVersion":"Retained unchanged when present; otherwise not-applicable."},
      "cleanupResult": "No DKMS registration, build, install, load, bind, or overlay change.",
      "diagnostics": ["installed kernel and missing header path/package", "preflight rejection", "cleanup latch false", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Evidence only.",
      "finalState": "Baseline unchanged and candidate non-live.",
      "residueAudit": "No staging, registration, package residue, or unexplained delta."
    },
    {
      "id": "overlay-or-resource-conflict", "representativeClass": "conflicting overlay or resource",
      "selection": "Supported system with a pre-existing non-package overlay marker or resource owner conflicting with the selected route.",
      "preconditions": "Conflict owner and baseline hashes recorded; conflict is not created by mutating unrelated software.",
      "failureInjection": "Attempt preflight/route selection against the recorded conflict.",
      "expected": {"compatibilityState":"Rejected","reason":"overlay-or-resource-conflict","liveEligible":false,"transactionState":"rejected-before-route-change","retainedPriorVersion":"Retained unchanged when present; otherwise not-applicable."},
      "cleanupResult": "No conflict removal, overwrite, route activation, bind, or ownership theft.",
      "diagnostics": ["conflict path/resource and owner", "selected and unselected route state", "cleanup latch false", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Evidence and removable staging only.",
      "finalState": "Foreign conflict and baseline preserved; candidate inactive.",
      "residueAudit": "No package residue, foreign change, claimed resource, or unexplained delta."
    },
    {
      "id": "interrupted-upgrade", "representativeClass": "interrupted upgrade",
      "selection": "Supported system with a complete predecessor and a distinct sealed successor.",
      "preconditions": "Predecessor snapshot and all durable checkpoints recorded; output disabled.",
      "failureInjection": "Interrupt separately after every durable upgrade checkpoint.",
      "expected": {"compatibilityState":"Unavailable","reason":"interrupted-transaction-recovery-required","liveEligible":false,"transactionState":"inactive-recovery-required","retainedPriorVersion":"Retained complete until each successor attempt is verified or recovered."},
      "cleanupResult": "Each attempt resumes only a proven checkpoint or converges to documented inactive predecessor state.",
      "diagnostics": ["operation ID and checkpoint", "predecessor/successor identities", "cleanup latch false after each recovery", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Exact journaled successor and transaction bytes only.",
      "finalState": "After every recovery, one known-complete inactive version and no mixed state.",
      "residueAudit": "Every checkpoint has a separate residue audit with no orphan, mixed-version byte, or unexplained delta."
    },
    {
      "id": "stale-manifest", "representativeClass": "stale manifest",
      "selection": "Installed candidate whose otherwise valid compatibility manifest names a different relevant release, kernel, UAPI, overlay, or enrollment identity.",
      "preconditions": "Current and stale identities recorded; no manifest rewrite during evaluation.",
      "failureInjection": "Substitute one stale relevant identity at a time.",
      "expected": {"compatibilityState":"Rejected","reason":"manifest-identity-stale","liveEligible":false,"transactionState":"rejected-before-load","retainedPriorVersion":"Retained unchanged when present; otherwise not-applicable."},
      "cleanupResult": "No load, bind, route activation, enrollment refresh, or fallback; stale attempt bytes removed.",
      "diagnostics": ["expected and observed manifest identities", "exact stale field and rejection", "cleanup latch false", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Evidence and removable staging only.",
      "finalState": "Prior inactive state retained; stale identity non-live.",
      "residueAudit": "No stale manifest installation, package residue, or unexplained delta."
    },
    {
      "id": "corrupted-archive-or-dtbo", "representativeClass": "corrupted archive or DTBO",
      "selection": "Sealed supported candidate with one deliberately corrupted source archive, GPIO4 DTBO, or GPIO20 DTBO per separate attempt.",
      "preconditions": "Original and corrupted hashes recorded; corruption occurs only in attempt-owned copies.",
      "failureInjection": "Flip bytes separately in the source archive, GPIO4 DTBO, and GPIO20 DTBO before verification.",
      "expected": {"compatibilityState":"Rejected","reason":"artifact-integrity-failed","liveEligible":false,"transactionState":"rejected-before-install","retainedPriorVersion":"Retained unchanged when present; otherwise not-applicable."},
      "cleanupResult": "Corrupt artifact never installs or activates; all corrupt copies removed.",
      "diagnostics": ["expected and observed artifact hashes for all three attempts", "artifact-specific verification rejection", "cleanup latch false", "no fallback selected"],
      "maximumDuration": "Declare a bounded deadline in each artifact attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and separate immutable evidence directory per corrupt artifact.",
      "allowedChanges": "Attempt-owned corrupt copies and evidence only.",
      "finalState": "Baseline unchanged; no corrupted artifact remains.",
      "residueAudit": "Per-attempt archive/GPIO4/GPIO20 residue audits show no package residue or unexplained delta."
    },
    {
      "id": "removal-inactive", "representativeClass": "removal while inactive",
      "selection": "Complete package-owned installation with no load, bind, open descriptor, owner, active generation, callback, DMA, or unsafe pin/clock state.",
      "preconditions": "Exact ownership inventory, shared/admin exclusions, and inactive hardware evidence complete.",
      "failureInjection": "none",
      "expected": {"compatibilityState":"Unavailable","reason":"package-completely-removed","liveEligible":false,"transactionState":"complete-removal-accepted","retainedPriorVersion":"No package-owned version retained."},
      "cleanupResult": "All and only package-owned state removed; shared/admin keys and configuration preserved.",
      "diagnostics": ["complete-removal acceptance fields", "module/DKMS/overlay/endpoint absence", "cleanup latch false", "preservation hashes"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and new immutable evidence directory.",
      "allowedChanges": "Removal of exact package-owned inventory only.",
      "finalState": "Package completely absent, inactive, and safe.",
      "residueAudit": "All package-owned residue absent; shared/admin and unrelated samples unchanged; no unexplained delta."
    },
    {
      "id": "removal-open-or-active", "representativeClass": "attempted removal while open or active",
      "selection": "Complete installation with one open endpoint or active/busy ownership assertion injected per separate attempt.",
      "preconditions": "Ownership and activity evidence recorded without producing live output.",
      "failureInjection": "Present open descriptor, owner, active generation, callback/DMA, or unsafe state individually.",
      "expected": {"compatibilityState":"Rejected","reason":"removal-precondition-unsafe","liveEligible":false,"transactionState":"removal-refused","retainedPriorVersion":"Installed version retained unchanged."},
      "cleanupResult": "Removal makes no package, module, overlay, boot, key, or unrelated change; separately authorized quiescence is required.",
      "diagnostics": ["exact blocking safety assertion", "removal refusal and unchanged inventory", "cleanup latch state classified", "bounded kernel-log delta"],
      "maximumDuration": "Declare a bounded deadline in each refusal attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, baseline, and separate immutable evidence directory per blocker.",
      "allowedChanges": "Evidence only.",
      "finalState": "Installation retained; no forced teardown; live eligibility disabled pending safe recovery.",
      "residueAudit": "Inventory unchanged, blocker still truthfully reported, and no unexplained delta."
    },
    {
      "id": "reinstall-after-removal", "representativeClass": "reinstall after complete removal",
      "selection": "Supported system that passed complete removal and has a proved empty package-owned baseline.",
      "preconditions": "First complete-removal evidence accepted; no retained journal, manifest, overlay, DKMS, enrollment, or cleanup state.",
      "failureInjection": "none",
      "expected": {"compatibilityState":"Compatible-unqualified","reason":"clean-reinstall-lifecycle-pass","liveEligible":false,"transactionState":"complete","retainedPriorVersion":"not-applicable; the proved empty baseline contains no prior package version."},
      "cleanupResult": "Reinstall verifies output-disabled identity, then a second complete removal returns to the proved empty baseline.",
      "diagnostics": ["empty pre-reinstall baseline", "fresh identities and default-disabled enrollment", "cleanup latch false", "second complete-removal acceptance"],
      "maximumDuration": "Declare a bounded deadline in the row attempt before mutation; exceeding it fails and invokes recovery.",
      "evidenceIdentity": "Bind one sealed candidate, exact system identity, authorization, proved empty baseline, and new immutable evidence directory.",
      "allowedChanges": "Fresh attempt-owned package state followed by its exact removal.",
      "finalState": "Second complete removal accepted; empty package-owned baseline restored.",
      "residueAudit": "No state inherited from the first installation and no residue or unexplained delta after the second removal."
    }
  ]
}
