<!-- SPDX-License-Identifier: MIT -->

# Phase 5.36 authorized output-disabled execution prompt

Bind the operator's authorization to the exact sealed Phase 5.36 schema-v3
control set at commit `47bff4f4e9780cc4b9d184de8cd759bf62dd3af8`
for frozen source commit `20f7a21ad8601f2e2fd4dec4640ea919acc22ce0`.
Authorization is limited to the 38 indexed attempts in the ten ready rows and
their authenticated pre-root, recovered-ledger handoff, 18-path Phase 5.31 to
Phase 5.36 tool transition, recovery, service, stock-kernel, DKMS, overlay,
load-disabled, query, unbind/rebind, unload, failure-injection, and cleanup
operations.

Before staging, recompute the execution-instance hash in the pre-root envelope
and repeat focused and complete validation. On wspr5 require the exact Phase
5.31 tool inventory, inactive baseline, exact canonical recovered ledger,
absent bound archive destination, checksummed release inputs, disconnected and
unused separate I2C Si5351 path, no antenna, unused SDR, and available recovery.

Execute only through the authenticated schema-v3 pre-root transition and
installed permanent tools. The prior ledger may move only to the exact bound
read-only archive path after the pre-root journal exists. Stop on the first
identity, ownership, mode, state, timeout, service, recovery, residue, cleanup,
or safety discrepancy. Use only sealed recovery authorized by authenticated
journals.

Output remains disabled. Prohibited operations include active pinctrl, clock
enablement, DMA submission, GPIO output, Si5351 operation, transmitter keying,
SDR operation, antenna connection, RF, `/dev/mem`, custom-kernel qualification,
forced removal, general upgrade, and unreviewed persistent boot mutation. Five
environmental rows remain deferred and are not authorized substitutes.
