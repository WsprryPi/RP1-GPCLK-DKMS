<!-- SPDX-License-Identifier: MIT -->

# Phase 5.33 Gate D control-set generation and validation prompt

Generate a distinct, complete, hash-closed Gate D control set for frozen
`0.0.0-phase5.33`, source commit
`4208941af537f21e3a20160d2d3d7fabe50f7cd3`, archive SHA-256
`e5f5a047b17c08e5b33aaedfd88385b560324649c236c3518df4a660371a6867`,
and representative module SHA-256
`e58b4b74b661eb2cfb50c4f960e9519dcfc9092769cf0c1947c9974906c24428`.
Preserve all Phase 5.32 controls, authorization, failure, and recovery evidence.

Create and mutually bind the Phase 5.33 route decision, target plan,
schema-version-2 qualification identity, bootstrap, deterministic 38-attempt
bundle and index, execution instance, and pre-root envelope. The qualification
identity must enumerate all 18 retained permanent paths with exact Phase 5.31
predecessor and Phase 5.33 successor hashes, including unchanged identities.
Rebind every candidate, archive, UAPI, sidecar, module, target-built helper,
copied tool, Python import, kernel, service, staging, qualification-root,
recovery, deadline, and safety identity.

Retain ten ready rows and five explicit environmental deferrals. GPIO4 and
GPIO20 remain distinct DKMS routes; Si5351 remains a prohibited separate I2C
output path. Control-set approval is offline only:
`targetExecutionApproved=false` and `executionReady=false`; readiness-required
validation must fail. No previous target authorization may be inherited.

Independently verify schema closure, source and installed hashes, release-input
colocation, transition uniqueness and completeness, installed import closure,
deterministic generation, 38 unique attempts, 15 interruption attempts, four
busy-removal attempts, and all fake executions. Run every attempt through a
copied installed executor's validation, planning, and pre-mutation authorization
gate. Mutate authorization, predecessor and successor identities, paths,
hashes, destinations, roles, attempts, and safety flags and require fail-closed
results.

This slice is offline only. Do not stage inputs on wspr5, install DKMS, load or
bind modules, activate overlays, change services or boot state, reboot, access
GPIO, enable clocks, submit DMA, operate Si5351, SDR, or transmitter, connect
an antenna, transmit, or produce RF. Run the complete offline suite, correct
every finding, commit attributable files, and push only the current upstream.
