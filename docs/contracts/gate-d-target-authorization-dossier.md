<!-- SPDX-License-Identifier: MIT -->

# Gate D target-authorization dossier status

This dossier is blocked and is not an authorization request. The successor,
predecessor, routes, wspr5 identity, recovery host, and deadlines are sealed in
the execution instance and version-pair contract, but 13 rows retain external
input blockers and `gate-d-instance --require-ready` fails.

Do not approve or execute a partial matrix. After exact representative-build,
newer-kernel, signature-enforcement, missing-header, and foreign-conflict inputs
are accepted, regenerate this dossier with the final execution-instance digest,
all operation-document digests, service actions, prerequisites, DKMS and module
actions, route attempts, planned reboots, failure injections, evidence paths,
cleanup states, rescue decisions, and physical confirmations.

The future request must retain immutable `live_output=0`, disconnected Si5351
leads, no antenna, unused SDRplay, and prohibitions on active pinctrl, clock
enablement, DMA submission, GPIO output, transmitter keying, RF, `/dev/mem`, a
custom kernel, forced removal, general upgrades, and unreviewed boot changes.
