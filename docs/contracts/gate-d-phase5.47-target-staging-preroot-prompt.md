<!-- SPDX-License-Identifier: MIT -->

# Phase 5.47 authenticated target staging and pre-root prompt

Execute only the live-target recapture, staging, and authenticated pre-root
transition authorized by commit
`ecfb65795f8a79a7d60264814c8fea2ac459d15d`. Bind this slice to frozen source
`c5320ac5419a04d17345370204524f219b7ff403`, archive SHA-256
`497368ac11b32e5491dab76103ad3bb2da0975c086e9898a801c5c2df1be82be`,
authorized execution-instance SHA-256
`9e0a1c74f2810670b8fb212b694dca2f9cc36f85259000afcf8d2b852c09fee8`,
schema-5 pre-root envelope SHA-256
`bf81b5e9085f40722ee45a6ced3a12e2052f8a238a8441b52eeb520dba409e5f`,
unchanged attempt-index SHA-256
`dc68030fa86386659f92a93f56a96d05979af2c541d1be7bfc3e3b33c2e4651d`,
and canonical snapshot SHA-256
`7f018fb331c769c44eb1691fdabc4a8a6ff22e0a3debc9e1ed69404d829332b0`.

Immediately before staging, run the committed read-only capture and the
independently implemented validator using the exact terminal recovery journal
and explicit physical declarations. Require raw byte identity with the
committed canonical snapshot: inactive runtime, all six services inactive,
terminal-complete Phase 5.46 administrator ledger, exact 28-path predecessor
inventory, authenticated terminal recovery, unused SDR, no antenna, and the
disconnected and unused separate I2C Si5351 path. Use `/usr/sbin/dkms` through
the capture tool; never retry with an unqualified `dkms` or `modinfo` command.
Remove transient recapture files. Stop before staging on any difference.

Only after an exact match, require the declared Phase 5.47 input directory to
be absent, create that directory, and populate all 62 envelope inputs from the
checksummed release unit and committed authorized controls, plus the separately
sealed envelope. Extract only the authenticated archive into its declared
path. Before transfer, independently enumerate the complete intended target
file set and reject AppleDouble `._*`, `.DS_Store`, VCS metadata, editor backup
files, caches, bytecode, and every path not derived from the sealed archive or
input manifest. Strip transport-level macOS extended attributes and resource
forks. After extraction, verify on the target every input hash and require the
complete regular-file path set to equal the independently enumerated set,
including all authenticated archive members and no extras. Run the exact staged
archived outer executor in read-only pre-root validation mode and require
success.

Invoke the authenticated schema-5 pre-root transition exactly once. It may
archive only the exact snapshot-bound Phase 5.46 administrator ledger, install
the exact Phase 5.47 qualification package, remove only declared runtime
residue, and create the authenticated qualification root. On failure, stop;
invoke only journal-authorized `--resume` recovery, then return without starting
an attempt.

After success, validate the immutable pre-root journal, installed permanent
executor using `/usr/libexec/rp1-gpclk-dkms/gate-d-executor`, qualification-root
marker, authorized execution instance, matrix policy, eight-module graph, and
attempt index. Use absolute target command paths, including `/usr/sbin/dkms`.
Stop before lifecycle attempt 1. Do not execute, resume, skip, or substitute
any indexed attempt in this slice.

Output remains disabled. Active pinctrl, clock enablement, DMA submission,
GPIO output, Si5351 operation, transmitter keying, SDR operation, antenna
connection, RF, `/dev/mem`, custom-kernel qualification, forced removal,
general upgrade, and unreviewed persistent boot mutation remain prohibited.

Preserve complete evidence, independently review the result, and commit and
push documentation only. Any identity, state, timeout, service, recovery,
residue, cleanup, transition, or safety discrepancy is a terminal stop.
