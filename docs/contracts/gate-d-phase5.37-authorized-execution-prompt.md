<!-- SPDX-License-Identifier: MIT -->

# Phase 5.37 authorized output-disabled execution prompt

Bind the operator's authorization to the exact sealed Phase 5.37 schema-v3
control set at commit `67ceb102ad670955a498344eb4899a98dd94f744`
for frozen source commit `71932324ec977d30ec0fadd48ef2673c49a6e173`.
Authorization is limited to the 38 indexed attempts in the ten ready rows and
their authenticated pre-root, recovered-ledger handoff, complete 22-path
retained-tool transition, recovery, service, stock-kernel, DKMS, overlay,
load-disabled, query, unbind/rebind, unload, failure-injection, and cleanup
operations.

Before staging, recompute the execution-instance hash in the pre-root envelope
and repeat focused and complete independent validation. On wspr5 require the
exact 22-path predecessor inventory, inactive baseline, canonical recovered
Phase 5.36 ledger, exact preserved Phase 5.34 archive, absent bound Phase 5.36
archive destination, checksummed release inputs, disconnected and unused
separate I2C Si5351 path, no antenna, unused SDR, and available recovery.

Execute only through the authenticated schema-v3 pre-root transition and
installed permanent tools. The canonical Phase 5.36 ledger may move only to
its exact bound read-only archive path after the pre-root journal exists. The
Phase 5.34 archive is immutable historical evidence. Stop on the first
identity, ownership, mode, state, timeout, service, recovery, residue, cleanup,
transition-membership, or safety discrepancy. Use only sealed recovery
authorized by authenticated journals.

Output remains disabled. Prohibited operations include active pinctrl, clock
enablement, DMA submission, GPIO output, Si5351 operation, transmitter keying,
SDR operation, antenna connection, RF, `/dev/mem`, custom-kernel
qualification, forced removal, general upgrade, and unreviewed persistent boot
mutation. Five environmental rows remain deferred and are not authorized
substitutes. Terminal pre-root recovery must return without beginning another
attempt.
