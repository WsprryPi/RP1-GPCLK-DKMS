<!-- SPDX-License-Identifier: MIT -->

# Phase 5.50 canonical live-target snapshot prompt

Before constructing Phase 5.50 controls, capture the current wspr5 predecessor
state with the reviewed read-only canonical snapshot tool. Stream the exact
tool bytes into privileged Python over authenticated SSH without creating a
target tool file. Require the terminal-complete Phase 5.48 administrator ledger
and sealed Phase 5.48 attempt journal, measured installed package paths, stock
kernel and canonical headers, non-enforcing signing state, inactive module,
endpoint, overlay, test DKMS versions, live output, and all six reviewed
services.

Bind the existing operator physical-safety declarations: the separate I2C
Si5351 path is disconnected and unused, the SDR is unused, and no antenna is
connected. The capture must not probe or operate GPIO, I2C, Si5351, SDR, clock,
DMA, antenna, transmitter, or RF hardware.

Separately inventory the retained Phase 5.50 representative-build evidence at
`/home/pi/gate-c-evidence/phase5.50-c241605`. Require exactly the seven sealed
regular release inputs, two expected regular helper binaries, and one extracted
archive root. Reject any extra top-level path, release input, link, special
file, metadata sidecar, cache, or unexpected artifact. Bind all seven release
input hashes to the representative-build manifest.

Capture the canonical snapshot twice and require byte equality. Independently
validate the exact canonical bytes and compare them to the Phase 5.49 snapshot.
The ledger, terminal journal, installed package inventory, kernel, services,
runtime, signing state, and physical declarations must remain identical unless
an explicitly explained and safe environmental identity changed. The retained
build directory is evidence only and must not be treated as an installed
package path.

This slice produces only the canonical Phase 5.50 predecessor snapshot,
retained-build inventory, and offline validation. Do not generate controls,
stage target inputs, request or consume lifecycle authorization, change
services, administer DKMS or a module, apply overlays, change boot state,
enable clocks, submit DMA, transmit, or produce RF. Commit and push only the
prompt, snapshot, retained-build inventory, review, and deterministic
validators.
