<!-- SPDX-License-Identifier: MIT -->

# Phase 5.47 Gate D control-set construction and validation prompt

Construct and independently validate the complete output-disabled Phase 5.47
Gate D control set for frozen source commit
`c5320ac5419a04d17345370204524f219b7ff403`, release archive SHA-256
`497368ac11b32e5491dab76103ad3bb2da0975c086e9898a801c5c2df1be82be`,
representative module SHA-256
`5c585105cfd5e11a83797bf34f52e9d0ed5a19c5f3ecf7bb74d771f01419ead3`,
and attempt namespace `phase5.47-c5320ac5419a`.

Capture a fresh canonical read-only snapshot on `wspr5` using the exact frozen
capture-tool bytes. Require the terminal-complete predecessor administrator
ledger, all measured installed paths, exact representative stock-kernel
identity, all six reviewed services inactive, and no module, endpoint, route
overlay, test DKMS version, or live output. Bind the declarations that the
separate I2C Si5351 path is disconnected and unused, the SDR is unused, and no
antenna is connected. Stop if capture or independent validation fails.

Generate deterministically the schema-5 route decision, target plan,
qualification bootstrap, execution instance, pre-root envelope, qualification
identity, predecessor inventory, attempt index, and all 38 attempt documents.
Retain ten ready rows and five deferred environmental rows. Every
attempt-owned path must be unique, strictly below the Phase 5.47 namespace,
and disjoint from every retained Phase 5.42, 5.43, 5.45, and 5.46 path.

Close the complete qualification-root graph. Every referenced control,
matrix policy, attempt index, attempt document, executor, and Python module
must be an authenticated pre-root input and sealed-root transition. Rebuild a
root only from those transitions and validate from that root. Independently
extract the executor and complete Python tool graph from the exact frozen
Phase 5.47 archive and require byte equality with the final envelope, plan,
index, and transitions. Do not validate archival identities from mutable
worktree substitutes.

Generate twice in isolated trees and require byte equality. Run the complete
archive-bound suite with Phase 5.43, 5.45, 5.46, and 5.47 archives. Correct
every actionable finding and repeat affected validation until clean. Record
offline-construction approval, but require `targetExecutionApproved` and
`executionReady` to remain false.

Do not stage lifecycle inputs, request or bind target authorization, change
services, administer DKMS, load or bind the module, apply an overlay, change
boot state, access GPIO or I2C, operate Si5351 or SDR hardware, enable clocks,
submit DMA, connect an antenna, transmit, or produce RF. Commit and push only
the deterministic controls, snapshot, validation, prompt, and review.
