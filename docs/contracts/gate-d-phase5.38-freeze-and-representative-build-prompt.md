<!-- SPDX-License-Identifier: MIT -->

# Phase 5.38 freeze and representative-build execution prompt

## Objective

Freeze development successor `0.0.0-phase5.38` from complete package-path
closure commit `5d8bab601c408f62284ba5512147873c3bf2fe21`. Preserve every Phase 5.37
control, authorization, staging, failure, recovery, and review artifact as
immutable history.

## Local freeze and reproducibility

Advance only active candidate versions, packaging paths, deterministic test
fixtures, and new Phase 5.38 release notes. Carry schema-3 archive-derived
package-path closure, exact existing-subset equality, typed file and symlink
identities, and recovery at all 28 observed boundaries. Do not reintroduce a
hand-maintained successor path list. Preserve the canonical UAPI, independent
GPIO4/GPIO20 administrative routes, and separate I2C Si5351 path.

Run focused closure tests and the complete offline suite. Independently review
version boundaries, package-layout expansion, duplicate destinations, archive
trees, transition/recovery typing, documentation claims, and historical
immutability. Correct actionable findings before freezing. Commit the clean
freeze, use its commit timestamp as `SOURCE_DATE_EPOCH`, and create two
isolated non-publishable development release units. Validate both and require
byte-for-byte identity.

## Exact wspr5 representative build

Transfer one checksummed release unit to a new Phase 5.38 evidence directory.
Require an inactive AArch64 stock-kernel baseline and canonical header path
with exact ownership and mode, `.config`, `Module.symvers`, architecture, and
compiler identity. Build the module plus Gate D UAPI probe and busy injector
directly and unprivileged. Record candidate archive, module, UAPI,
administrator, diagnostics, pre-root module, outer executor, helper,
configuration, `Module.symvers`, compiler, vermagic, ELF, staging-directory,
and final inactive-state identities.

## Safety boundary

Do not administer DKMS, install package files, mutate live ledgers, replace
retained package paths, load or bind a module, activate an overlay, alter boot
or services, reboot, access GPIO, enable clocks, submit DMA, operate the
separate Si5351, use SDR/transmitter equipment, connect an antenna, transmit,
or produce RF.

## Exit criteria

Exit only with a clean freeze, byte-identical validated release units, a
passing exact representative stock-kernel build, matching inactive baselines,
no actionable adversarial finding, pushed commits, and a clean synchronized
worktree. A new Phase 5.38 Gate D control set remains a separate next gate and
is not authorized by this build evidence.
