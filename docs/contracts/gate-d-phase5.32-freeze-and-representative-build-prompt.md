<!-- SPDX-License-Identifier: MIT -->

# Phase 5.32 freeze and representative-build execution prompt

Create successor `0.0.0-phase5.32` from the exact executor repair and
pre-freeze installed-CLI rehearsal. Preserve Phase 5.31 source, release,
control-set, qualification-root, authorization, and target-failure evidence.

Update only active candidate version identities, packaging paths, release
notes, lifecycle model data, and deterministic tests required for the distinct
successor. Do not rewrite historical Phase 5.31 controls or evidence. Run the
installed-CLI rehearsal across all 38 indexed attempts and the complete offline
suite. Perform an adversarial version-boundary review and correct every
actionable finding.

Commit the implementation and version transition. From that exact clean commit,
produce two isolated non-publishable release units, validate each independently,
and require every generated artifact to be byte-identical. Record source commit,
archive and DTBO hashes, tool identities, and validation results.

Transfer only the exact checksummed release unit needed for an unprivileged,
build-only representative check on `wspr5`. Reconfirm the stock kernel and
canonical headers, compile the extracted module directly against those headers,
and record configuration, `Module.symvers`, compiler, architecture, module
version, module hash, vermagic, and file identity. Verify the final target
baseline.

Do not perform DKMS add/build/install, install files, load or bind a module,
activate an overlay, change services or boot state, reboot, access GPIO, enable
clocks, submit DMA, operate the separate I2C Si5351 path, use a transmitter or
SDR, connect an antenna, transmit, or produce RF. Stop after representative
build evidence and its adversarial assessment. Gate D control-set generation,
target installation, lifecycle execution, tagging, publication, pull requests,
and dependent-repository changes are later gates.
