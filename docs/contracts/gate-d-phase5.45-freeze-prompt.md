<!-- SPDX-License-Identifier: MIT -->

# Phase 5.45 candidate-freeze prompt

Freeze the next development successor as `0.0.0-phase5.45` from the clean
phase-scoped-path prerequisite commit
`7e8909cfe6e888d7f0e3022f01dd57fc500cfcce`. Update only active module,
packaging, release-policy, diagnostic, lifecycle, and test identities that
track the current development release. Add Phase 5.45 behavior and security
notes describing the candidate-bound path namespace and collision prevention.

Preserve every historical Phase 5.42 and Phase 5.43 control, archive identity,
review, authorization, attempt bundle, execution record, and retained-evidence
contract byte-for-byte. Do not regenerate a control set or claim a target
build from provisional hashes.

Require exact agreement among `dkms.conf`, the module version header, active
release layout, installation/removal contracts, representative matrix,
diagnostics, administrator tooling, and current-version tests. Run whitespace,
version-pair, SPDX, schema, documentation-link, deterministic historical
control-set, phase-scoped collision, and complete offline checks. Perform a
separate adversarial diff review and correct every actionable finding.

Commit and push the exact clean freeze. Do not build or stage target artifacts,
connect to `wspr5`, install or remove DKMS state, operate a module or overlay,
change services or boot state, use GPIO, enable clocks, submit DMA, operate the
separate I2C Si5351 path, use an SDR, connect an antenna, transmit, or produce
RF. The exact representative build is a later slice bound to the resulting
freeze commit.
