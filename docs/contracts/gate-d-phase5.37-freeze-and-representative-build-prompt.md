<!-- SPDX-License-Identifier: MIT -->

# Phase 5.37 freeze and representative-build execution prompt

## Objective

Freeze `0.0.0-phase5.37` from complete retained-tool transition successor
commit `dbdfc681331a7cc6e8aeda97c6315378fbf88e10`. Preserve every Phase
5.36 control, authorization, staging, failure, recovery, and review artifact as
immutable history.

## Local freeze and reproducibility

Advance only active candidate versions, packaging paths, deterministic test
fixtures, and new Phase 5.37 release notes. Carry the closed 22-path retained
permanent-tool inventory and pre-transaction completeness check without
changing the canonical UAPI, GPIO4/GPIO20 administrative route separation, or
the separate I2C Si5351 path.

Run the focused successor test, complete offline suite, and an independent
adversarial review. Commit the clean freeze. Use its commit timestamp as
`SOURCE_DATE_EPOCH` to generate two isolated, non-publishable development
release units. Validate each independently and require byte-for-byte identity.

## Exact wspr5 representative build

Transfer one checksummed unit to a new Phase 5.37 evidence directory. Require
an inactive AArch64 stock-kernel baseline and the canonical header path with
safe ownership and mode, `.config`, `Module.symvers`, and compiler identity.
Build the module and Gate D helper binaries directly and unprivileged. Record
the module, UAPI, administrator, diagnostics, pre-root, outer-executor, helper,
configuration, and final inactive-state identities.

## Safety boundary

Do not administer DKMS, install package files, move or alter live ledgers,
replace retained tools, load or bind a module, activate an overlay, alter
services or boot state, reboot, access GPIO, enable a clock, submit DMA,
operate the separate Si5351 path, use SDR/transmitter equipment, connect an
antenna, transmit, or produce RF.

## Exit criteria

Exit only with a clean freeze, two byte-identical validated release units, a
passing exact representative build, matching inactive baselines, no actionable
adversarial finding, pushed commits, and a clean synchronized worktree. A new
Phase 5.37 Gate D control set remains the next separate gate.
