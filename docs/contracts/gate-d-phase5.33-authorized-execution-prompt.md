<!-- SPDX-License-Identifier: MIT -->

# Phase 5.33 authorized output-disabled execution prompt

Bind the operator's fresh authorization to the exact reviewed Phase 5.33 Gate D
control set for frozen source commit
`4208941af537f21e3a20160d2d3d7fabe50f7cd3`. Authorization is limited to the
38 indexed attempts in the ten ready rows and their sealed pre-root bootstrap,
18-path predecessor/successor tool transition, recovery, service,
stock-kernel switching, DKMS, overlay, load-disabled, query, unbind/rebind,
unload, failure-injection, and cleanup actions.

Recompute the execution-instance and envelope identities, repeat independent
validation, and commit and push those exact authorized bytes before staging.
On `wspr5`, require the reviewed inactive baseline, exact release and retained
tool hashes, separate disabled I2C Si5351 path, no antenna, unused SDR, and
available rescue/power recovery. Preserve all prior evidence.

Execute only through the authenticated pre-root transition and permanent
tools. Stop on the first identity, state, service, timeout, recovery, residue,
cleanup, or safety discrepancy. Never improvise commands, paths, kernels, or
recovery. Preserve every failed or interrupted journal immutably. If a failure
occurs, run only the exact sealed recovery path when its authenticated ledgers
authorize it, then stop and reassess before any further attempt.

Output must remain disabled. Prohibited operations include active pinctrl,
clock enablement, DMA submission, GPIO output, Si5351 operation, transmitter
keying, SDR operation, antenna connection, RF, `/dev/mem`, custom-kernel
qualification, forced removal, general upgrade, and unreviewed boot mutation.
Five environmental rows remain deferred and are not authorized substitutes.
