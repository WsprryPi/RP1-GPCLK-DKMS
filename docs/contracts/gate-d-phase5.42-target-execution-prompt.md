<!-- SPDX-License-Identifier: MIT -->

# Phase 5.42 authenticated target-execution prompt

Execute only the authorized Phase 5.42 output-disabled control set committed at
`dd9b04cc24f23c6e61f562e41a1287560b46ed39`. Before any target staging,
recompute and verify the authorized execution-instance, pre-root envelope,
attempt-index, release archive, canonical snapshot, recapture attestation, and
all input-file hashes.

Rehearse the exact frozen release archive's staged outer executor and pre-root
module against the authorized schema-5 envelope. Require those exact archived
bytes—not development-worktree substitutes—to accept the envelope and its
current-ledger archival, predecessor-inventory, successor-inventory, snapshot,
and recovery contracts. Stop before target access if the exact archived tool
graph cannot validate the authorized envelope.

Only after that passes, require a byte-identical live snapshot, stage the exact
sealed inputs in the new Phase 5.42 input directory, validate through the
authenticated pre-root executor, and invoke the privileged transition. Then
execute only the 38 indexed attempts through installed permanent tools,
stopping on the first discrepancy and using only journal-authorized recovery.

Output remains disabled. Prohibit active pinctrl, clock enablement, DMA
submission, GPIO output, Si5351 operation, transmitter keying, SDR operation,
antenna connection, RF, `/dev/mem`, custom-kernel qualification, forced
removal, general upgrade, and unreviewed persistent boot mutation.

Preserve complete evidence, independently review the result, and commit and
push documentation only. Never patch a frozen archive, staged tool, envelope,
or authorized hash in place.
