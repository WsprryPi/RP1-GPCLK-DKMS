<!-- SPDX-License-Identifier: MIT -->

# Phase 5.41 freeze, representative build, and control-set prompt

Freeze successor `0.0.0-phase5.41` from the Phase 5.40 signing-policy repair at
commit `7a0ae79831c256caa1997abcfd0bc8b5e87d1da3`. Preserve all Phase 5.39
execution, failure, terminal-recovery, and historical control-set evidence
unchanged. Advance only active candidate identities, deterministic fixtures,
release paths, and new Phase 5.41 release notes.

Run focused freeze checks and the complete offline suite. Commit and push the
clean freeze. Use the exact freeze commit timestamp as `SOURCE_DATE_EPOCH` to
create two isolated, non-publishable release units; validate both and require
byte identity. Transfer one checksummed unit to a new `wspr5` evidence
directory. Verify the exact AArch64 stock-kernel headers, configuration,
compiler, repaired non-enforcing signing policy, completed Phase 5.39 terminal
attestation, and inactive output-disabled baseline. Build the module and both
Gate D helpers directly and unprivileged from the archive. Record exact input,
environment, transcript, and output hashes and independently verify the final
inactive baseline.

Generate a distinct, deterministic, hash-closed Phase 5.41 schema-3/schema-4
Gate D control set grounded in the exact representative artifacts and complete
typed predecessor-package inventory. Require all 38 indexed attempts, ten ready
rows, five deferred environmental rows, the complete retained-tool transition,
recovery boundaries, canonical package digest, exact release inputs, and the
repaired signing-policy evidence contract. Keep target execution disabled.
Re-generate into a clean temporary tree, require byte identity, run focused and
complete offline validation, and perform a separate adversarial review. Correct
and retest every actionable finding.

Do not install, add, build, remove, or administer DKMS; install or replace
package paths; mutate ledgers; load, bind, unbind, or unload a module; activate
or remove overlays; alter boot or services; reboot; access GPIO; enable clocks;
submit DMA; operate the separate I2C Si5351 path; operate SDR or transmitter
equipment; connect an antenna; transmit; or produce RF. Do not stage or begin a
lifecycle execution. That remains a separate digest-bound authorization gate.

Exit only with a clean pushed freeze, byte-identical release units, passing
exact representative build, deterministic complete control set, independent
clean review, pushed evidence/control commits, and a clean synchronized
worktree.
