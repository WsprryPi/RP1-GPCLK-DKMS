<!-- SPDX-License-Identifier: MIT -->

# Phase 5.34 Gate D control-set generation and validation prompt

Generate a distinct, complete, hash-closed Gate D control set for frozen
`0.0.0-phase5.34`, source commit
`3a3f970739934ead0f49629d0a9cda8113b33357`, archive SHA-256
`a9895836700f284fc8e2e89c58a7b2cbd9257ea60543ebe1f59cddd2a2359ae6`,
and representative module SHA-256
`2250172cd8430d05bb1aab147308128e69157df65bf0288532de210266cfc70d`.
Preserve every Phase 5.33 control, authorization, failure, journal, and recovery
artifact as historical evidence.

Create and mutually bind the Phase 5.34 route decision, target plan,
schema-version-2 qualification identity, bootstrap, deterministic 38-attempt
bundle and index, execution instance, and pre-root envelope. Enumerate all 18
permanent paths. Source every predecessor hash by path from the last successful
Phase 5.31 retained-tool manifest and compare all 18 paths read-only on wspr5.
Source successor identities only from the frozen Phase 5.34 archive and exact
target-built helper evidence. Unchanged paths remain explicit.

Rebind every candidate, archive, UAPI, release sidecar, module, target helper,
copied tool, Python import, kernel, service, staging, qualification-root,
recovery, deadline, and safety identity. The administrator state path is
`/var/lib/rp1-gpclk-dkms/transaction.json`; pre-root recovery is terminal.

Retain ten ready rows and five environmental deferrals. GPIO4 and GPIO20 remain
distinct routes, and the Si5351 remains a separate prohibited I2C output path.
Control approval is offline only: `targetExecutionApproved=false` and
`executionReady=false`; readiness-required validation must fail.

Independently verify schema closure, transition uniqueness, installed import
closure, deterministic attempt generation, 38 unique attempts, 15 interruption
attempts, four busy-removal attempts, and all fake executions. Run every attempt
through a copied executor's validate, plan, and pre-mutation authorization gate.
Mutate authorization, identities, paths, roles, hashes, destinations, attempts,
and safety fields and require fail-closed results.

This slice permits only offline repository work and the explicit read-only
retained-tool inventory. Do not stage execution inputs, install or administer
DKMS, load or bind modules, activate overlays, change services or boot state,
reboot, access GPIO, enable clocks, submit DMA, operate Si5351, SDR, or a
transmitter, connect an antenna, transmit, or produce RF. Run the complete
offline suite, correct every finding, commit attributable files, and push.
