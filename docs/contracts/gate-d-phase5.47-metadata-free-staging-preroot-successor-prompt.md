<!-- SPDX-License-Identifier: MIT -->

# Phase 5.47 metadata-free staging and pre-root successor prompt

Repeat the authorized Phase 5.47 staging and authenticated pre-root slice from
the beginning after the failed-closed attempt recorded at
`da7cb93b62c2b825c7afa72417546de1420e8a0d`. Retain every identity, boundary,
physical declaration, prohibition, and stop condition in
`gate-d-phase5.47-target-staging-preroot-prompt.md`.

Construct a fresh staging tree solely from the 62 envelope-declared inputs,
the separately sealed envelope, and regular-file members of the authenticated
release archive. Independently derive and freeze that exact 669-file allowlist.
Reject AppleDouble `._*`, `.DS_Store`, `.git` or other VCS metadata, editor
backups, caches, Python bytecode, sockets, devices, FIFOs, unsafe links, path
traversal, and every undeclared path.

Create the transfer archive with macOS copyfile/resource-fork handling disabled
and without extended attributes. Before transfer, inspect both its member set
and PAX metadata; require exact equality to the allowlist, no forbidden names,
and no `LIBARCHIVE.xattr`, `SCHILY.xattr`, or other extended-attribute keys.
Transfer only that archive and a separately reviewed read-only verifier.

On wspr5, first repeat the canonical read-only capture and require raw byte
identity with snapshot
`7f018fb331c769c44eb1691fdabc4a8a6ff22e0a3debc9e1ed69404d829332b0`.
Require the Phase 5.47 staging path, qualification root, and pre-root journal to
be absent. Extract into the one declared staging path, then independently
verify all 62 input hashes, envelope SHA-256
`bf81b5e9085f40722ee45a6ced3a12e2052f8a238a8441b52eeb520dba409e5f`,
and exact 669-file target allowlist with no extras.

Only after every check passes, run the exact archived executor in read-only
pre-root mode, then invoke the authenticated schema-5 pre-root transition once.
If it fails, use only journal-authorized recovery and return. If it succeeds,
validate the terminal journal, root marker, installed permanent executor,
authorized instance, matrix policy, eight-module graph, attempt index, inactive
runtime, and six inactive services. Use absolute paths including
`/usr/sbin/dkms`, `/usr/sbin/modinfo` if ever required by a sealed tool, and
`/usr/libexec/rp1-gpclk-dkms/gate-d-executor`.

Stop before lifecycle attempt 1. Output remains disabled. GPIO output, active
pinctrl, clock enablement, DMA submission, Si5351 or SDR operation, antenna
connection, transmission, RF, `/dev/mem`, forced removal, general upgrade, and
unreviewed persistent boot mutation remain prohibited.
