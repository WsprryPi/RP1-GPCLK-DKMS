<!-- SPDX-License-Identifier: MIT -->

# Phase 5.38 Gate D control-set generation and validation prompt

Generate a distinct hash-closed Gate D control set for frozen development
candidate `0.0.0-phase5.38` at commit
`639a4667f081886c6bc068f5eb87ab3373130baf`, archive SHA-256
`518b90d084c8184ed09b7b8b2bd7ca0b9e1ab607548226df72ad93b3ad2985ff`,
and representative module SHA-256
`caf229dfd30f1102fa3c51a5926285aefb65e21bbd9aa9bf5fdaed283a7a999f`.
Preserve every earlier control, authorization, execution, failure, recovery,
and review artifact unchanged.

Create and mutually bind the route decision, target plan, schema-3
qualification identity, bootstrap, 38-attempt bundle and index, execution
instance, and pre-root envelope. Derive the package-path universe from the
sealed archive layout. Bind the exact existing 28-path predecessor subset on
`wspr5`: 22 regular permanent-tool files, four regular operator-documentation
files, and two command symlinks. Bind file type, predecessor and successor
hash, mode, owner, and group; bind symlink type, exact predecessor and
successor relative target, owner, and group. Reject omissions, additions,
duplicates, wrong types, wrong identities, and layout drift before mutation.

Bind the canonical recovered Phase 5.37 administrator ledger at
`/var/lib/rp1-gpclk-dkms/transaction.json`, SHA-256
`24af8111eaa7e9f0c5084dd39063160a5188195a73667b3fcbf115c3c4ea64cf`,
root ownership, mode `0600`, `status=recovered`,
`recoveryRequired=false`, and `liveOutput=false`. Bind the unique absent Phase
5.37 archive destination at mode `0400`. Preserve and authenticate the Phase
5.36 and Phase 5.34 read-only archives without cleanup authority.

Validate the live predecessor inventory read-only. Require deterministic
generation, exact schema/import/path closure, 38 unique fake executions, 15
interruption attempts, four busy-removal attempts, copied-executor validation
and planning, package-transition equality, history continuity, and the
authorization gate. Mutate ledger fields, paths, types, identities, ownership,
modes, link targets, roles, hashes, transition membership, authorization, and
safety values and require fail-closed results.

Keep `targetExecutionApproved=false` and `executionReady=false`; general
validation must pass and readiness-required validation must fail. Do not stage
target inputs, move ledgers, administer DKMS, replace package paths, load or
bind modules, activate overlays, alter services or boot, access GPIO, enable
clocks, submit DMA, operate the separate I2C Si5351, use SDR/transmitter
equipment, reboot, transmit, or produce RF. Run the complete offline suite,
independently review, commit attributable files, and push. Fresh explicit
authorization remains required for target execution.
