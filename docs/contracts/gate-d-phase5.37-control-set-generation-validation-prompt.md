<!-- SPDX-License-Identifier: MIT -->

# Phase 5.37 Gate D control-set generation and validation prompt

Generate a distinct hash-closed Gate D control set for frozen
`0.0.0-phase5.37` at `71932324ec977d30ec0fadd48ef2673c49a6e173`,
archive SHA-256
`299e8abe61f7c4ee81d9431539dcbae3614d33be2dafca0cb94a83996a4146ef`,
and representative module SHA-256
`d34c6369ff56c3aa281f023fe5b2f044c409a2117c2120e115bc536703a52add`.
Preserve all earlier controls, authorizations, failures, recoveries, and review
evidence unchanged.

Create and mutually bind the route decision, target plan, schema-version-2
qualification identity, bootstrap, 38-attempt bundle and index, execution
instance, and schema-version-3 pre-root envelope. Bind all 22 permanent-tool
successors from the frozen archive or target-built helpers and all 22 exact
predecessors from the live retained inventory. The four paths omitted by Phase
5.36—`gate-d-attempts`, `gate-d-residue`, `lifecycle-policy`, and
`rp1-gpclk-diagnostics`—must be first-class transitions.

Bind the canonical recovered Phase 5.36 administrator ledger at
`/var/lib/rp1-gpclk-dkms/transaction.json`, SHA-256
`1ee3c83cbd88d8980ee0be5b1514939a8bc66953b74d966a5a6151f295e6a51e`,
root ownership, mode `0600`, `status=recovered`,
`recoveryRequired=false`, and `liveOutput=false`. Bind the unique absent
archive destination
`/var/lib/rp1-gpclk-dkms/history/phase5.36-transaction-recovered.json` at mode
`0400`. Require the already preserved Phase 5.34 archive to remain exact and
untouched.

Validate the ledger history and all 22 predecessor tools read-only on wspr5.
Verify schema closure, exact import closure, deterministic generation, 38
unique fake executions, 15 interruption attempts, four busy-removal attempts,
copied-executor validation/planning, complete-transition equality, and the
authorization gate. Mutate ledger fields, paths, identities, roles, hashes,
transition membership, authorization, and safety values and require
fail-closed results.

Keep `targetExecutionApproved=false` and `executionReady=false`; the general
instance remains valid but readiness-required validation must fail. Do not
stage target inputs, move either ledger, administer DKMS, load or bind modules,
activate overlays, change services or boot, access GPIO, clocks, or DMA,
operate Si5351/SDR/transmitter equipment, reboot, transmit, or produce RF. Run
the complete offline suite, independently review, commit attributable files,
and push.
