<!-- SPDX-License-Identifier: MIT -->

# Release 1.0.0 repaired target-verification authorization prompt

I explicitly authorize the single remaining Release 1.0.0 target-verification
sequence on `wspr5`, bound to repaired qualification-control commit
`9e54d88e9f4c9eac79a77c72e60b39c3acb4e6fa`, installed unchanged Debian
product package
`951289ee5d0e44cff41b59756f00161aba16f43f1450715ba57c4a3679a2e6b8`,
and repaired qualification archive
`c05f2f2adc20b9e99bf37d775c4bddd6cafd27e5da5e9c62410784fb835727d2`.

I freshly confirm that the Si5351 path is physically disconnected and that no
antenna or transmitter is connected to either GPIO4/GPCLK or GPIO20/GPCLK.

Begin with one final read-only recapture. Require the installed `1.0.0-1`
package, empty package audit, four stock-kernel DKMS installations, exact UAPI
and both DTBO identities, unloaded module, absent endpoint, zero active or
boot-selected project overlays, inactive conflicting services, executable
command paths, and both physical-safety confirmations. Stop without mutation
on any mismatch.

Only after a match, create one new user-owned staging directory, transfer the
unchanged product and repaired qualification artifact without metadata, verify
their exact outer hashes and complete checksum set, require the exact regular
archive-member inventory, extract it, and validate and render the
archive-contained controls. Do not reuse or patch the prior staged controls.

Execute the repaired remaining plan once: verify the current inactive package;
perform exactly one GPIO4 `live_output=0` lifecycle using the authoritative
before/after overlay-listing delta for identifier capture and scoped cleanup;
repeat exactly once for GPIO20; perform exactly one complete conventional
package removal and residue audit; reinstall the same unchanged product package
exactly once; and prove the final inactive baseline. On failure, perform only
the repaired control's attempt-scoped cleanup, preserve evidence, restore a
package-manager-consistent inactive state if the bounded path permits it, and
stop without retry or improvisation. Remove both old and new user-owned staging
directories only after evidence is sealed and only if their exact identities
match this sequence.

This authorization permits the named package removal/reinstallation,
output-disabled module load/unload, GPIO4 and GPIO20 runtime overlay
apply/remove with safe inactive pinctrl binding, and bounded UAPI
query/acquire/release. It does not permit `live_output=1`, clock enable or rate
change, DMA submission, GPIO output, boot changes, reboot, transmission, RF,
tag creation, publication, or consumer-repository changes. Stop after sealing
the remaining exact-candidate verification evidence.
