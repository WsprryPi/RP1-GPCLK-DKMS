<!-- SPDX-License-Identifier: MIT -->

# Phase 5.45 authenticated target staging and pre-root prompt

Execute only the staging and pre-root portion of the Phase 5.45 authorization
recorded at commit `362c469175578154aecfe159a25d3db8e6a91fe9`. Bind the
slice to frozen source `4b50db7868b7fe5ca9d830f51cd404c250192188`, archive
SHA-256
`21d05675e9d12ddb4c051868578c410737b733786357cee20eb2b0ce03f63356`,
authorized execution-instance SHA-256
`0a4e2b88263262d408aa30c39e4843aa1204735333cedf6bb472dfc1a50ef228`,
schema-5 pre-root envelope SHA-256
`1a01c76d95e06fae7a132b05c3dc5d1ef3db1c71ea4e00fc4f7d6a10cc686742`,
unchanged attempt-index SHA-256
`3375c809dd699949f991742716628016a680bcf7253fc30ba8f3de52c294f020`,
and canonical snapshot SHA-256
`66208586a112792e91185a7ce67d5952427dc218fb8a392ac1bfb221ed51e4c8`.

Immediately before staging, use the committed read-only capture tool with the
same terminal recovery journal and physical declarations. Independently
validate the result and require raw byte identity with the 7,057-byte canonical
snapshot. Require the inactive runtime, six inactive services, terminal
complete Phase 5.43 ledger, exact 28-path predecessor inventory, authenticated
recovery, disconnected and unused separate I2C Si5351 path, unused SDR, no
antenna, and available recovery. Remove transient capture files.

Only after an exact match, create the one new input directory declared by the
authorized envelope. Populate every declared input from the checksummed Phase
5.45 release unit and committed control set, plus the separately sealed
envelope document. Verify the exact path set and every SHA-256 on the target.
Run the exact archived outer executor in read-only pre-root validation mode and
require success before privileged execution.

Invoke the authenticated schema-5 pre-root transition once. It may archive
only the exact snapshot-bound Phase 5.43 administrator ledger, install the
exact Phase 5.45 qualification package, remove only declared runtime residue,
and create the authenticated qualification root. On failure, stop. Invoke only
journal-authorized `--resume` recovery, and return without starting an attempt.

After success, validate the immutable pre-root journal, installed permanent
executor, qualification-root marker, authorized execution instance, and
attempt index. Stop before lifecycle attempt 1. Do not execute, resume, skip,
or substitute any indexed attempt in this slice.

Output remains disabled. Active pinctrl, clock enablement, DMA submission,
GPIO output, Si5351 operation, transmitter keying, SDR operation, antenna
connection, RF, `/dev/mem`, custom-kernel qualification, forced removal,
general upgrade, and unreviewed persistent boot mutation remain prohibited.

Preserve complete evidence, independently review the result, and commit and
push documentation only. Any identity, state, service, recovery, residue,
cleanup, transition, or safety discrepancy is a terminal stop for this slice.
