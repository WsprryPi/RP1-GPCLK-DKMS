<!-- SPDX-License-Identifier: MIT -->

# Phase 5.53 split-artifact Gate D control-set prompt

Capture `wspr5` at least twice with exact frozen source
`1884c0f1c53c661495576bf10ce08d8bf7a90bc3` read-only canonical snapshot
tool bytes streamed directly into privileged Python. Bind the existing physical
declarations: the separate I2C Si5351 path is disconnected and unused, the SDR
is unused, and no antenna is connected. Require byte-equal captures, stock
kernel and headers, six inactive services, inactive module, endpoint, overlay,
DKMS test versions and live output, terminal administrator state, and the
sealed predecessor recovery journal.

Construct the complete output-disabled Phase 5.53 lifecycle control set from
the exact product archive, separate qualification archive, representative
module, offline-checks-twice evidence, and canonical snapshot. Bind all eight
release inputs, including an explicit qualification-archive role. Generate 38
schema-2 attempts in a unique Phase 5.53 namespace, ten ready matrix rows, five
deferred environmental rows, and the complete sealed qualification-root graph.
Require `inputsReady=true` while `approved`, `targetExecutionApproved`, and
`executionReady` remain false.

Generate twice and require byte equality. Reconstruct the qualification root
using only frozen candidate bytes and require the exact pre-root validator to
accept both archives and the entire release-input graph. Fail closed without
retrofitting or bypassing a frozen validator if it cannot represent the split.

This slice permits only read-only snapshot capture and offline construction.
Do not stage target inputs, perform a pre-root transition, request or consume
authorization, administer DKMS or a module, change overlays, services, or boot
state, access GPIO or I2C, enable clocks, submit DMA, operate Si5351 or SDR
hardware, connect an antenna, transmit, or produce RF.
