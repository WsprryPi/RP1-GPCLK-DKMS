<!-- SPDX-License-Identifier: MIT -->

# Phase 5.31 Gate D control-set generation and validation prompt

Generate a distinct, complete, hash-closed Gate D control set for frozen
`0.0.0-phase5.31`, source commit
`c7e6fafdc434bdad78e12a6683239b8d87845cfa`, archive SHA-256
`ac0ce593e988886a24c22866409a20097a24105a94b846152bdc30ac4a060bed`,
and representative module SHA-256
`7e1e02a535c6b549327411c84e48580c31efb0e0b1662e7fd3b3bc58f31a44b9`.
Preserve Phase 5.30 controls, authorization, failure, and recovery evidence.

Create and mutually bind a route decision, target plan, qualification identity
and bootstrap, deterministic 38-attempt bundle and index, execution instance,
and pre-root envelope. Rebind every source, archive, UAPI, sidecar, module,
tool, kernel, staging, qualification-root, rescue, service, deadline, and
authorization identity. Retain ten ready rows and five explicit environmental
deferrals. GPIO4 and GPIO20 remain separate routes; the Si5351 remains a
prohibited, separate I2C path. Target execution must remain unauthorized.

Independently validate closed schemas and fields, all hashes and paths,
transition uniqueness and completeness, release-input colocation, installed
tool/import closure, deterministic regeneration, 38 unique attempts, 15
interruption attempts, four busy-removal attempts, and all fake executions.
Adversarially mutate authorization, identities, roles, paths, hashes,
destinations, routes, attempts, and safety flags; require fail-closed results.

This slice is offline only. Do not run target commands, install DKMS, load or
bind modules, activate overlays, change services or boot state, reboot, access
GPIO, enable clocks, submit DMA, operate the Si5351, SDR, or transmitter,
connect an antenna, or produce RF. Run the complete offline suite, correct
every finding, commit attributable files, push only the current upstream, and
report identities, checks, Git state, and the separately authorized next gate.
