<!-- SPDX-License-Identifier: MIT -->

# Phase 5.32 authorized output-disabled execution prompt

Bind the operator's fresh authorization to the exact reviewed Phase 5.32 Gate D
control set for frozen source commit `4e62b3a0b584396a9528be07592d92e0796555f2`.
Authorization is limited to the 38 indexed attempts in the ten ready rows and
their reviewed bootstrap, recovery, service, stock-kernel switching, DKMS,
overlay, load-disabled, query, unbind/rebind, unload, failure-injection, and
cleanup actions.

Recompute the execution-instance and self-authenticating envelope identities,
repeat independent validation, and commit and push those exact authorized
bytes before target staging. On `wspr5`, require the reviewed inactive baseline,
exact release hashes, separate disabled and isolated I2C Si5351 path, no antenna,
unused SDR, and available rescue/power recovery. Preserve all prior evidence.

Execute only through the authenticated permanent tools and exact qualification
root. Stop on the first identity, state, service, timeout, recovery, residue,
cleanup, or safety discrepancy. Never improvise commands, paths, kernels, or
recovery. Preserve every failed or interrupted journal immutably.

Output must remain disabled. Prohibited operations include active pinctrl,
clock enablement, DMA submission, GPIO output, Si5351 operation, transmitter
keying, SDR operation, antenna connection, RF, `/dev/mem`, custom-kernel
qualification, forced removal, general upgrade, and unreviewed boot mutation.
Five environmental rows remain deferred and are not authorized substitutes.
