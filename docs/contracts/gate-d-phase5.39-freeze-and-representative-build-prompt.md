<!-- SPDX-License-Identifier: MIT -->

# Phase 5.39 freeze and representative-build execution prompt

Freeze development successor `0.0.0-phase5.39` from typed Gate D inventory
integration commit `ac4893a8f0f3bdc441bc7254da1a4fc0598b9f4b`. Preserve all Phase
5.38 freeze, build, blocker, and review evidence unchanged.

Advance only active version identities, release paths, deterministic fixtures,
and new release notes. Carry qualification identity schema 3 plus bootstrap and
pre-root schema 4 typed package inventories and their shared canonical digest.
Preserve the canonical UAPI, independent GPIO4/GPIO20 administrative routes,
and separate I2C Si5351 path.

Run focused typed-inventory tests and the complete offline suite. Commit the
clean freeze. Use its commit timestamp as `SOURCE_DATE_EPOCH` to create two
isolated non-publishable release units; validate both and require byte identity.
Transfer one checksummed unit to a new `wspr5` evidence directory, verify the
inactive AArch64 stock-kernel/header/configuration/compiler baseline, and build
the module and two Gate D helpers directly and unprivileged. Record exact
artifact identities and verify the final inactive baseline.

Do not administer DKMS, install or replace package paths, mutate ledgers, load
or bind a module, activate overlays, alter boot or services, reboot, access
GPIO, enable clocks, submit DMA, operate the separate Si5351, use SDR or
transmitter equipment, connect an antenna, transmit, or produce RF.

Exit only with a clean freeze, byte-identical release units, a passing exact
representative build, matching inactive baselines, an independent adversarial
assessment, pushed commits, and a clean synchronized worktree. Phase 5.39
control-set generation remains the next separate gate.
