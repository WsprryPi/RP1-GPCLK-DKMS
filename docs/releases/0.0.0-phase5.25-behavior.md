<!-- SPDX-License-Identifier: MIT -->

# 0.0.0-phase5.25 behavior notes

Phase 5.25 closes the pre-root administrator input graph over the source
archive, both route DTBOs, compatibility manifest, provenance, release
metadata, and checksum manifest. The pre-root coordinator validates those
files at their exact administrator-visible paths before mutation.

Pre-root recovery now records whether administrator invocation began and
distinguishes absent, exact, and unsafe administrator transaction state.
Already-clean recovery is repeatable. Module runtime behavior is unchanged.
