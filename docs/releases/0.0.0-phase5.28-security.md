<!-- SPDX-License-Identifier: MIT -->

# 0.0.0-phase5.28 security

Only the canonical `/lib` alias is allowlisted. The real module tree and header
target remain contained, non-symlink, correctly owned, and non-writable by
group or world. All unrelated installation paths retain strict symlink denial.
