<!-- SPDX-License-Identifier: MIT -->

# 0.0.0-phase5.51 security

The schema-6 repair expands only the permanent executor's explicit supported
instance set and preserves the complete qualification-root marker,
ownership/mode, target-plan, installed-tool, and Python import-graph trust
checks. Unknown schemas and mismatched plan schemas continue to fail closed.

No authorization, output enablement, GPIO, clock, DMA, Si5351, SDR,
transmission, or RF capability is added by this candidate.
