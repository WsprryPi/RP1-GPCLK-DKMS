<!-- SPDX-License-Identifier: MIT -->

# 0.0.0-phase5.21 security notes

The outer executor performs a standard-library trust bootstrap over the
instance preamble, qualification-root marker, target-plan hash, and exact
root-validator source or installed bytes. Verified bytes are compiled directly
into the imported module, avoiding a verify-then-reopen race.
